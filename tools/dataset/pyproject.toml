[project]
name = "neoglossa-dataset"
version = "0.1.0"
description = "Build-time tooling that produces NeoGlossa's read-only lexicon"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
